services:
  - type: web
    name: omus-todo-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    disk:
      name: todo-db
      mountPath: /data
      sizeGB: 1
